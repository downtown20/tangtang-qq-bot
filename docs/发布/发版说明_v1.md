# 🍬 小糖糖 v1.5

> 修好歌唱工作室——它此前在任何机器上都跑不起来，而且失败是静默的。

**下载 `tangtang-v1.5.zip`，解压后双击 `安装糖糖.bat`。**
她是一个在 QQ 上活着的 AI 伙伴：有独立于消息而存在的自我状态，会积累记忆、维护关系，
自己决定说不说话。**LLM 提供瞬间，架构提供生命。**

---

## 选择下载

| 文件 | 说明 |
| --- | --- |
| `tangtang-v1.5.zip` | 主包（约 1.1 G）——聊天、记忆、41 首预录歌曲、角色贴图、图形控制台 |
| `tangtang-singing-models.zip` | 翻唱制作组件（约 400 M，**可选**，见文末） |

语音引擎与声线模型（约 6 G）为分卷，挂在 [v1.0](../../releases/tag/v1.0)——勾了「发语音」时
安装程序自动拉取，不必手动下载。

## 快速开始

1. 解压 `tangtang-v1.5.zip`，双击 **`安装糖糖.bat`**，勾选需要功能
   - 默认已勾好：聊天+高质量记忆 / 唱歌 / 图形控制台
   - 想让她开口说话 → 勾「发语音+听懂语音消息」（模型约 6.5 G，自动下载）
   - 想让她看懂图 → 勾「本地识图」（约 2.5 G；也可走云端千问 VL，零下载）
2. 双击 **`启动控制台.bat`**，填机器人 QQ 号、你自己的 QQ 号、模型 API Key
3. 控制台里点「启动 SnowLuma」扫码登录 → 点「启动糖糖」

> **还差一步**：SnowLuma 是第三方 QQ 协议端，许可协议不允许随本项目分发。
> 需从 [官方发布页](https://github.com/SnowLuma/SnowLuma/releases) 下载 Windows x64 版，
> 解压到 `SnowLuma/` 目录下。安装程序开头会提醒；也可用任意其他 OneBot 11 实现替代。

断网直接重跑 `安装糖糖.bat`，已下载的部分不会重来；事后补装同样重跑、勾上对应项。

## 本次更新

- **fix(唱歌)** 歌唱工作室依赖链卡在 NumPy 2——`faiss` / `pyworld` / `numba`
  都是 NumPy 1.x 编译的，导入即报错。requirements 改钉
  `numpy>=1.23.5,<1.24` + `faiss-cpu==1.7.4`
- **fix(唱歌)** RVC 索引在中文路径下静默加载失败——faiss 的 C++ 端用窄字符 `fopen`，
  绝对路径含中文时打不开；索引不生效也不报错，只是音色相似度降低。改为传相对路径
- **fix(控制台)** 「打开目录」四个按钮原先只弹「找不到目录：`<路径>`」，
  未说明原因与下一步。现按功能分别说明，并在歌唱工作室顶部预告缺失组件
- **fix(控制台)** 密钥管理第二项 `千问 VL API Key` → `识图 API Key`
  （选本地识图不需要 Key）
- **fix(安装器)** 「唱歌」选项说明补上「仅播放，不含翻唱制作工具」

更早版本见 `docs/发布/更新日志.md` 与[历史 Release](../../releases)。

## 关于 `tangtang-singing-models.zip`

做新翻唱时才需要。**点歌播放不需要它**——41 首成品已随包自带。

包内是精简过的 RVC 推理代码与 HuTao 模型（原目录 12.8 G，只保留推理要用的 96 个文件，
训练记录与数据集已剥离），解压到项目根目录即可。人声分离所需的 `audio-separator`
环境与自动安装通道尚未接上，将在后续版本补齐。

## 文档

- `README.md` —— 她是怎么活的、工程上怎么保证这些设计不腐化
- `docs/用户手册/使用说明.md` —— 详细使用说明
- `docs/模块地图.md` —— 代码地图：74 个业务模块按职责分七层
